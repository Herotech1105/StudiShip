<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <title>StudyShip</title>
    <link rel="icon" href="./icons/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="./styles/styles.css">
    <link rel="stylesheet" href="./styles/dashboard.css">

</head>
    <body>
        <header>
            <nav>
                <a href="/" class="logo">StudyShip 📖</a>
                <div class="mobile-menu">
                    <a href="/createRoom">Erstellen</a>
                    <a href="/search">Suchen</a>
                    <form action="/logout" method="POST" class="logout-form">
                        <button type="submit" class="logout-link-style">Abmelden</button>
                    </form>
                </div>
            </nav>
        </header>
        <main>
            <h3>Dashboard</h3>
                {{#if user }}
                    <h4>Wilkommen, {{user}}!</h4>
                {{/if}}
            <a href="/search" class="search-btn">Suchen</a>
            <h4>Deine Lerngruppen: </h4>
            <a href="/createRoom" class="add-group-btn">Gruppe Erstellen</a>
            <ul>
                {{#each rooms}}
                    <li >
                        <a href="/rooms?roomId={{this.id}}" class="room-selection-btn">
                            <p>{{this.name}}</p>
                            <p>{{this.subject}}</p><!--js:is probably missing here-->
                            <p>{{this.owner}}</p>
                        </a>
                    </li>
                {{else}}
                    <p>Du bist bis in noch in keiner Gruppe</p>
                {{/each}}
            </ul>
        </main>
        <footer>
            <p>Diese Seite wurde erstellt von Barnabas Steiner, Tim ...</p>
            <p>Nur zur internen Verwendung an der DHBW Ravensburg Campus Friedrichshafen.</p>
        </footer>
    </body>
</html>